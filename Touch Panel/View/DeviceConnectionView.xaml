<UserControl x:Class="Touch_Panel.View.DeviceConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Touch_Panel.View"
             >
    <Grid Margin="10, 0, 0, 0" Height="15" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="9"/>
            <ColumnDefinition Width="13"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Black" BorderThickness="1">
            <Rectangle x:Name="Tx"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Rectangle.Style>
                    <Style TargetType="Rectangle">
                        <Setter Property="Fill" Value="Gray"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding TxSent}" Value="True">
                                <Setter Property="Fill" Value="Yellow"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding TxSent}" Value="False">
                                <Setter Property="Fill" Value="Gray"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Rectangle.Style>
            </Rectangle>
        </Border>
        <Border BorderBrush="Black" BorderThickness="1" Grid.Row="1" >
            <Rectangle x:Name="Rx" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                <Rectangle.Style>
                    <Style TargetType="Rectangle">
                        <Setter Property="Fill" Value="Gray"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RxReceived}" Value="True">
                                <Setter Property="Fill" Value="Yellow"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding RxReceived}" Value="False">
                                <Setter Property="Fill" Value="Gray"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Rectangle.Style>
            </Rectangle>
        </Border>
        <Border BorderBrush="Black" BorderThickness="0,1,1,1" Grid.RowSpan="2" Grid.Column="1"  >
            <Rectangle x:Name="Open" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Rectangle.Style>
                    <Style TargetType="Rectangle">
                        <Setter Property="Fill" Value="Red"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Connected}" Value="True">
                                <Setter Property="Fill" Value="GreenYellow"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding Connected}" Value="False">
                                <Setter Property="Fill" Value="Red"/>
                            </DataTrigger>  
                  </Style.Triggers>
                    </Style>
                </Rectangle.Style>
            </Rectangle>
        </Border>
        <Label x:Name="lbPortName" Content="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
            Grid.RowSpan="2" Grid.Column="2"
            FontSize="10"
            Foreground="White"
            FontWeight="DemiBold"
            VerticalAlignment="Center"
            Padding="5,0"/>
    </Grid>
</UserControl>
