<UserControl x:Class="Touch_Panel.View.DeviceConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Touch_Panel.View"
             >
    <UserControl.Resources>
        <local:UpperCaseConverter x:Key="Upper"/>
    </UserControl.Resources>
    <Grid Margin="10, 0, 0, 0" Height="15" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="9"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Black" BorderThickness="1">
            <Rectangle x:Name="Tx"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Rectangle.Style>
                    <Style TargetType="Rectangle">
                        <Setter Property="Fill" Value="Gray"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding TxSent}" Value="True">
                                <Setter Property="Fill" Value="Yellow"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding TxSent}" Value="False">
                                <Setter Property="Fill" Value="Gray"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Rectangle.Style>
            </Rectangle>
        </Border>
        <Border BorderBrush="Black" BorderThickness="1" Grid.Row="1" >
            <Rectangle x:Name="Rx" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                <Rectangle.Style>
                    <Style TargetType="Rectangle">
                        <Setter Property="Fill" Value="Gray"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RxReceived}" Value="True">
                                <Setter Property="Fill" Value="Yellow"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding RxReceived}" Value="False">
                                <Setter Property="Fill" Value="Gray"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Rectangle.Style>
            </Rectangle>
        </Border>
        <!-- Chữ tên: LimeGreen khi kết nối mở, trắng khi đóng -->
        <Label x:Name="lbPortName" Content="{Binding Name, Converter={StaticResource Upper}, UpdateSourceTrigger=PropertyChanged}"
            Grid.RowSpan="2" Grid.Column="1"
            FontSize="11"
            FontWeight="SemiBold"
            VerticalAlignment="Center"
            Padding="5,0">
            <Label.Style>
                <Style TargetType="Label">
                    <Setter Property="Foreground" Value="White"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Connected}" Value="True">
                            <Setter Property="Foreground" Value="LimeGreen"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Label.Style>
        </Label>
    </Grid>
</UserControl>
